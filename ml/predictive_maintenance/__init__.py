"""Predictive maintenance — failure-within-horizon classifier.

Import the training entrypoint lazily via ``ml.predictive_maintenance.train`` to keep
``python -m ml.predictive_maintenance.train`` free of double-import warnings.
"""
