# Python deps for the Airflow retraining DAG (Phase 7).
# Pinned to match the `ml` package's runtime requirements so DAG tasks reproduce the
# same training/inference behaviour as a local `python -m ml.pipeline` run.
mlflow==2.16.2
scikit-learn>=1.5
xgboost>=2.1
pandas>=2.2
pyarrow>=16.1
boto3==1.34.162
python-dotenv>=1.0
